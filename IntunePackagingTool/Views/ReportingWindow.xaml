<Window x:Class="IntunePackagingTool.Views.ReportingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Installation Report" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#34495E" Padding="24,16">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊" FontSize="24" Foreground="White" Margin="0,0,12,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Name="AppNameHeader" Text="Application Name" 
                                   FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Installation Statistics and Device Status" 
                                   FontSize="12" Foreground="#BDC3C7"/>
                    </StackPanel>
                </StackPanel>

                <Button Name="RefreshButton" Content="🔄 Refresh" 
                        HorizontalAlignment="Right"
                        Background="#3498DB" Foreground="White"
                        BorderThickness="0" Padding="12,8"
                        Click="RefreshButton_Click"
                        Style="{DynamicResource {x:Static ToolBar.ButtonStyleKey}}"/>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="1" Margin="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Devices Card -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" 
                    Margin="0,0,8,0" Padding="16" 
                    BorderBrush="#E1E8ED" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="📱" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Name="TotalDevicesText" Text="0" 
                               FontSize="28" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#2C3E50"/>
                    <TextBlock Text="Total Devices" FontSize="11" 
                               HorizontalAlignment="Center" Foreground="#7F8C8D"/>
                </StackPanel>
            </Border>

            <!-- Successful Card -->
            <Border Grid.Column="1" Background="#E8F8F5" CornerRadius="8" 
                    Margin="0,0,8,0" Padding="16"
                    BorderBrush="#27AE60" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="✅" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Name="SuccessfulText" Text="0" 
                               FontSize="28" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#27AE60"/>
                    <TextBlock Name="SuccessRateText" Text="Installed (0%)" FontSize="11" 
                               HorizontalAlignment="Center" Foreground="#27AE60"/>
                </StackPanel>
            </Border>

            <!-- Failed Card -->
            <Border Grid.Column="2" Background="#FDEDEC" CornerRadius="8" 
                    Margin="0,0,8,0" Padding="16"
                    BorderBrush="#E74C3C" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="❌" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Name="FailedText" Text="0" 
                               FontSize="28" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#E74C3C"/>
                    <TextBlock Name="FailureRateText" Text="Failed (0%)" FontSize="11" 
                               HorizontalAlignment="Center" Foreground="#E74C3C"/>
                </StackPanel>
            </Border>

            <!-- Pending Card -->
            <Border Grid.Column="3" Background="#FEF5E7" CornerRadius="8" 
                    Margin="0,0,8,0" Padding="16"
                    BorderBrush="#F39C12" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="⏳" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Name="PendingText" Text="0" 
                               FontSize="28" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#F39C12"/>
                    <TextBlock Text="Pending" FontSize="11" 
                               HorizontalAlignment="Center" Foreground="#F39C12"/>
                </StackPanel>
            </Border>

            <!-- Not Installed Card -->
            <Border Grid.Column="4" Background="#F8F9FA" CornerRadius="8" 
                    Padding="16"
                    BorderBrush="#95A5A6" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="⚫" FontSize="24" HorizontalAlignment="Center"/>
                    <TextBlock Name="NotInstalledText" Text="0" 
                               FontSize="28" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="#95A5A6"/>
                    <TextBlock Text="Not Installed" FontSize="11" 
                               HorizontalAlignment="Center" Foreground="#95A5A6"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Device List -->
        <Border Grid.Row="2" Background="White" Margin="24,0,24,16" 
                CornerRadius="8" BorderBrush="#E1E8ED" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- List Header -->
                <Border Background="#F8F9FA" Padding="16,12" 
                        BorderBrush="#E1E8ED" BorderThickness="0,0,0,1">
                    <Grid>
                        <TextBlock Text="Device Installation Details" 
                                   FontSize="16" FontWeight="SemiBold"/>

                        <!-- Filter ComboBox -->
                        <ComboBox Name="StatusFilter" 
                                  HorizontalAlignment="Right"
                                  Width="180"
                                  SelectionChanged="StatusFilter_SelectionChanged">
                            <ComboBoxItem Content="All Statuses" IsSelected="True"/>
                            <ComboBoxItem Content="✅ Installed Only"/>
                            <ComboBoxItem Content="❌ Failed Only"/>
                            <ComboBoxItem Content="⏳ Pending Only"/>
                            <ComboBoxItem Content="⚫ Not Installed"/>
                        </ComboBox>
                    </Grid>
                </Border>

                <!-- Search Bar -->
                <Border Grid.Row="1" Padding="16,8" Background="#FAFBFC">
                    <TextBox Name="SearchBox" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             TextChanged="SearchBox_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="White" 
                                                    BorderBrush="#DEE2E6" 
                                                    BorderThickness="1" 
                                                    CornerRadius="4">
                                                <Grid>
                                                    <TextBlock Text="🔍 Search by device or user name..." 
                                                               Foreground="#ADB5BD"
                                                               Margin="8,8,0,0"
                                                               VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <ScrollViewer x:Name="PART_ContentHost" Margin="8,8"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Border>

                <!-- Data Grid -->
                <DataGrid Grid.Row="2" Name="DeviceStatusGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          VerticalScrollBarVisibility="Auto">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA"/>
                            <Setter Property="Foreground" Value="#495057"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#DEE2E6"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="" 
                                            Binding="{Binding StatusIcon}" 
                                            Width="40"
                                            IsReadOnly="True"/>

                        <DataGridTextColumn Header="Device Name" 
                                            Binding="{Binding DeviceName}" 
                                            Width="2*"
                                            IsReadOnly="True"/>

                        <DataGridTextColumn Header="User" 
                                            Binding="{Binding UserPrincipalName}" 
                                            Width="2*"
                                            IsReadOnly="True"/>

                        <DataGridTextColumn Header="Install Status" 
                                            Binding="{Binding InstallState}" 
                                            Width="*"
                                            IsReadOnly="True"/>

                        <DataGridTemplateColumn Header="Status Details" Width="250" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DetailedStatus}" 
                           TextWrapping="Wrap"
                           VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding InstallState}" Value="Failed">
                                                        <Setter Property="Foreground" Value="#E74C3C"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding InstallState}" Value="Installed">
                                                        <Setter Property="Foreground" Value="#27AE60"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                        <TextBlock.ToolTip>
                                            <ToolTip>
                                                <StackPanel MaxWidth="400">
                                                    <TextBlock FontWeight="Bold" Text="{Binding ErrorCodeDisplay}"/>
                                                    <TextBlock Text="{Binding DetailedStatus}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                                    <TextBlock Text="Recommended Action:" FontWeight="Bold" Margin="0,4,0,0"/>
                                                    <TextBlock Text="{Binding RecommendedAction}" TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </ToolTip>
                                        </TextBlock.ToolTip>
                                </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Last Sync" 
                                            Binding="{Binding FormattedLastSync}" 
                                            Width="150"
                                            IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F8F9FA" Padding="16,12" 
                BorderBrush="#E1E8ED" BorderThickness="0,1,0,0">
            <Grid>
                <TextBlock Name="StatusText" Text="Loading installation statistics..." 
                           FontSize="12" Foreground="#6C757D"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Name="ExportButton" Content="📥 Export to CSV" 
                            Margin="0,0,8,0"
                            Background="#6C757D" Foreground="White"
                            BorderThickness="0" Padding="12,6"
                            Click="ExportButton_Click"
                            Style="{DynamicResource {x:Static ToolBar.ButtonStyleKey}}"/>

                    <Button Name="CloseButton" Content="Close" 
                            Background="#495057" Foreground="White"
                            BorderThickness="0" Padding="16,6"
                            Click="CloseButton_Click"
                            Style="{DynamicResource {x:Static ToolBar.ButtonStyleKey}}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>