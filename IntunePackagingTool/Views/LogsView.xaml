<UserControl x:Class="IntunePackagingTool.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="White" Padding="20" Margin="20,20,20,10"
                BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#FF6B6B" Width="40" Height="40" CornerRadius="8" Margin="0,0,15,0">
                        <TextBlock Text="📋" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock Text="Intune Management Extension Diagnostics" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="View logs and troubleshoot application installations" FontSize="12" Foreground="#666" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Grid Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Computer Name:" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <TextBox Grid.Column="1" Name="ComputerNameTextBox" 
                             VerticalAlignment="Center"
                             Padding="8"
                             Text="{Binding ComputerName, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding GetLogsCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <Button Grid.Column="2" Name="GetLogsButton" 
                            Content="📥 Get Logs" 
                            Click="GetLogsButton_Click"
                            Margin="10,0,5,0"
                            Padding="15,8"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                   
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <!-- Loading Overlay -->
            <Border Name="LoadingOverlay" 
                    Background="#80000000" 
                    Visibility="Collapsed"
                    CornerRadius="8"
                    Margin="20,0">
                <StackPanel HorizontalAlignment="Center" 
                            VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" 
                                 Width="200" Height="4"
                                 Foreground="#4CAF50"/>
                    <TextBlock Text="Retrieving logs..." 
                               Foreground="White"
                               FontSize="14"
                               Margin="0,10,0,0"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Tab Control for different log views -->
            <TabControl Name="LogTabControl" Margin="20,10,20,10" Visibility="Collapsed">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" 
                                            Margin="2,0"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            BorderThickness="0">
                                        <ContentPresenter x:Name="ContentSite"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"
                                                          ContentSource="Header"
                                                          Margin="12,8"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="White"/>
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#E0E0E0"/>
                                            <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                

               

                <!-- Log Files Tab -->
                <TabItem Header="📁 Log Files">
                    <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="0,8,8,8">
                        <DataGrid Name="LogFilesGrid" 
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                 >
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="File Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Size" Binding="{Binding SizeFormatted}" Width="100"/>
                                <DataGridTextColumn Header="Last Modified" Binding="{Binding LastModified}" Width="200"/>
                                <DataGridTemplateColumn Header="Actions" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="📖 View" 
                                                        Tag="{Binding}"
                                                        Click="ViewLogFile_Click"
                                                        Margin="2"
                                                        Padding="5,2"/>
                                                <Button Content="💾 Save" 
                                                        Tag="{Binding}"
                                                        Click="SaveLogFile_Click"
                                                        Margin="2"
                                                        Padding="5,2"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>
            </TabControl>

            <!-- Empty State -->
            <Border Name="EmptyState" 
                    Background="White" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    CornerRadius="8"
                    Margin="20,10"
                    Padding="40">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="🔍" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                    <TextBlock Text="No logs loaded" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               HorizontalAlignment="Center" 
                               Margin="0,10,0,5"/>
                    <TextBlock Text="Enter a computer name and click 'Get Logs' to view diagnostics" 
                               FontSize="14" 
                               Foreground="#666" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="15,10" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <TextBlock Name="StatusText" Text="Ready" VerticalAlignment="Center" Foreground="#666"/>
                <TextBlock Name="LastUpdatedText" Text="" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#666"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>