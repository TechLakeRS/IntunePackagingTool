<UserControl x:Class="IntunePackagingTool.Views.WingetCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IntunePackagingTool.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400"
             Background="#F5F7FA">

    <UserControl.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E4E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="2" Opacity="0.1" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0056B3"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004494"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#6C757D"/>
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F9FA"/>
                    <Setter Property="BorderBrush" Value="#ADB5BD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Input Field Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007BFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Style="{StaticResource ModernCard}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Winget Catalog Browser" 
                             FontSize="24" FontWeight="SemiBold" 
                             Foreground="#212529" Margin="0,0,0,8"/>
                    <TextBlock Text="Search and install applications from the Windows Package Manager catalog" 
                             FontSize="14" Foreground="#6C757D"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Total Packages: " FontSize="14" Foreground="#6C757D" VerticalAlignment="Center"/>
                    <TextBlock Name="TotalPackagesText" Text="0" FontSize="16" FontWeight="SemiBold" 
                             Foreground="#007BFF" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search and Filter Section -->
            <Border Grid.Row="0" Style="{StaticResource ModernCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Search Bar -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Name="SearchTextBox" 
                               Grid.Column="0"
                               Style="{StaticResource ModernTextBox}"
                               Height="40"
                               FontSize="14"
                               KeyDown="SearchTextBox_KeyDown"
                               VerticalContentAlignment="Center"/>

                        <!-- Placeholder Text -->
                        <TextBlock Grid.Column="0" 
                                 Text="Search for packages..." 
                                 Foreground="#ADB5BD"
                                 FontSize="14"
                                 Margin="12,0,0,0"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Left"
                                 IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Button Name="SearchButton" 
                              Grid.Column="1"
                              Content="🔍 Search" 
                              Style="{StaticResource PrimaryButton}"
                              Click="SearchButton_Click"
                              Width="100"
                              Height="40"
                              Margin="8,0,0,0"/>

                        <Button Name="RefreshButton" 
                              Grid.Column="2"
                              Content="🔄 Refresh" 
                              Style="{StaticResource SecondaryButton}"
                              Click="RefreshButton_Click"
                              Width="100"
                              Height="40"
                              Margin="4,0,0,0"/>
                    </Grid>

                    <!-- Filters -->
                    <Grid Grid.Row="1" Margin="0,16,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Source" FontSize="12" Foreground="#6C757D" Margin="0,0,0,4"/>
                            <ComboBox Name="SourceFilter" 
                                    Style="{StaticResource ModernComboBox}"
                                    SelectionChanged="SourceFilter_SelectionChanged">
                                <ComboBoxItem Content="All Sources" IsSelected="True"/>
                                <ComboBoxItem Content="winget"/>
                                <ComboBoxItem Content="msstore"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,8,0">
                            <TextBlock Text="Category" FontSize="12" Foreground="#6C757D" Margin="0,0,0,4"/>
                            <ComboBox Name="CategoryFilter" 
                                    Style="{StaticResource ModernComboBox}"
                                    SelectionChanged="CategoryFilter_SelectionChanged">
                                <ComboBoxItem Content="All Categories" IsSelected="True"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="0,0,8,0">
                            <TextBlock Text="Sort By" FontSize="12" Foreground="#6C757D" Margin="0,0,0,4"/>
                            <ComboBox Name="SortBy" 
                                    Style="{StaticResource ModernComboBox}"
                                    SelectionChanged="SortBy_SelectionChanged">
                                <ComboBoxItem Content="Name" IsSelected="True"/>
                                <ComboBoxItem Content="Publisher"/>
                                <ComboBoxItem Content="Version"/>
                                <ComboBoxItem Content="Downloads"/>
                            </ComboBox>
                        </StackPanel>

                        <CheckBox Name="ShowInstalledOnly" 
                                Grid.Column="4"
                                Content="Show Installed Only"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Checked="ShowInstalledOnly_Changed"
                                Unchecked="ShowInstalledOnly_Changed"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- Package List -->
            <Border Grid.Row="1" Style="{StaticResource ModernCard}">
                <Grid>
                    <DataGrid Name="PackageDataGrid"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            HeadersVisibility="Column"
                            GridLinesVisibility="None"
                            BorderThickness="0"
                            Background="White"
                            RowHeight="60"
                            SelectionMode="Single"
                            SelectionChanged="PackageDataGrid_SelectionChanged"
                            MouseDoubleClick="PackageDataGrid_MouseDoubleClick">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F8F9FA"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E7F3FF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F8F9FA"/>
                                <Setter Property="Foreground" Value="#495057"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#DEE2E6"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <!-- Icon Column -->
                            <DataGridTemplateColumn Header="" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Width="32" Height="32" Margin="8" CornerRadius="4" Background="#F0F0F0">
                                            <TextBlock Text="📦" FontSize="16" 
                                                     HorizontalAlignment="Center" 
                                                     VerticalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Name and Publisher Column -->
                            <DataGridTemplateColumn Header="Package" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Margin="8,0">
                                            <TextBlock Text="{Binding Name}" 
                                                     FontWeight="Medium" 
                                                     FontSize="14"
                                                     Foreground="#212529"/>
                                            <TextBlock Text="{Binding Publisher}" 
                                                     FontSize="12"
                                                     Foreground="#6C757D"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- ID Column -->
                            <DataGridTextColumn Header="Package ID" 
                                              Binding="{Binding Id}"
                                              Width="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="8,0"/>
                                        <Setter Property="FontFamily" Value="Consolas"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="Foreground" Value="#495057"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Version Column -->
                            <DataGridTextColumn Header="Version" 
                                              Binding="{Binding Version}"
                                              Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="8,0"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="Foreground" Value="#495057"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Source Column -->
                            <DataGridTextColumn Header="Source" 
                                              Binding="{Binding Source}"
                                              Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="Foreground" Value="#6C757D"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Status Column -->
                            <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="12" 
                                              Padding="8,4"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                                                            <Setter Property="Background" Value="#D1F2EB"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                                                            <Setter Property="Background" Value="#F8F9FA"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="11" FontWeight="Medium">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                                                                <Setter Property="Text" Value="Installed"/>
                                                                <Setter Property="Foreground" Value="#10B981"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                                                                <Setter Property="Text" Value="Available"/>
                                                                <Setter Property="Foreground" Value="#6C757D"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Action Column -->
                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="📦" 
                                                  ToolTip="Install"
                                                  Click="InstallButton_Click"
                                                  Tag="{Binding}"
                                                  Style="{StaticResource PrimaryButton}"
                                                  Height="30"
                                                  Width="35"
                                                  Padding="4"
                                                  FontSize="14"
                                                  Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}"/>
                                            <Button Content="ℹ️" 
                                                  ToolTip="Details"
                                                  Click="DetailsButton_Click"
                                                  Tag="{Binding}"
                                                  Style="{StaticResource SecondaryButton}"
                                                  Height="30"
                                                  Width="35"
                                                  Padding="4"
                                                  FontSize="14"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Loading Indicator -->
                    <Grid Name="LoadingPanel" Visibility="Collapsed" Background="#FFFFFF99">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,12"/>
                            <TextBlock Text="Loading packages..." FontSize="14" Foreground="#6C757D"/>
                        </StackPanel>
                    </Grid>

                    <!-- No Results Message -->
                    <TextBlock Name="NoResultsText" 
                             Text="No packages found matching your criteria"
                             Visibility="Collapsed"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             FontSize="16"
                             Foreground="#6C757D"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F8F9FA" BorderThickness="0,1,0,0" BorderBrush="#DEE2E6">
            <Grid Height="40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Name="StatusText" 
                         Grid.Column="0"
                         Text="Ready"
                         VerticalAlignment="Center"
                         Margin="16,0"
                         FontSize="13"
                         Foreground="#6C757D"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0">
                    <TextBlock Text="Showing " VerticalAlignment="Center" FontSize="13" Foreground="#6C757D"/>
                    <TextBlock Name="ShowingCountText" Text="0" VerticalAlignment="Center" FontSize="13" 
                             Foreground="#495057" FontWeight="Medium"/>
                    <TextBlock Text=" of " VerticalAlignment="Center" FontSize="13" Foreground="#6C757D"/>
                    <TextBlock Name="TotalCountText" Text="0" VerticalAlignment="Center" FontSize="13" 
                             Foreground="#495057" FontWeight="Medium"/>
                    <TextBlock Text=" packages" VerticalAlignment="Center" FontSize="13" Foreground="#6C757D"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>