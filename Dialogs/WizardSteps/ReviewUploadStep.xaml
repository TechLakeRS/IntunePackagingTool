<UserControl x:Class="IntunePackagingTool.WizardSteps.ReviewUploadStep"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Review Configuration" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- App Details Review -->
                <Border Background="#F8F9FA" CornerRadius="6" Padding="20" Margin="0,0,0,15"
        BorderBrush="#E1E8ED" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="📱 Application Details" FontWeight="SemiBold" 
                   FontSize="16" Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <!-- First set of fields -->
                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                <ColumnDefinition Width="*"/>
                                <!-- Separator -->
                                <ColumnDefinition Width="20"/>
                                <!-- Second set of fields -->
                                <ColumnDefinition Width="Auto" MinWidth="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Left Column -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Foreground="#7F8C8D"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Name="AppNameText"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Name="DescriptionText" TextWrapping="Wrap" Margin="0,8,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Version:" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Name="VersionText" Margin="0,8,0,0"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Publisher:" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Name="PublisherText" Margin="0,8,0,0"/>


                            <!-- Right Column -->
                            <TextBlock Grid.Row="0" Grid.Column="3" Text="Install Context:" Foreground="#7F8C8D"/>
                            <TextBlock Grid.Row="0" Grid.Column="4" Name="InstallContextText"/>

                            <TextBlock Grid.Row="1" Grid.Column="3" Text="Install Cmd:" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="4" Name="InstallCommandText" FontFamily="Consolas" FontSize="10" TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="3" Text="Uninstall Cmd:" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="4" Name="UninstallCommandText" FontFamily="Consolas" FontSize="10" TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>

                            <TextBlock Grid.Row="3" Grid.Column="3" Text="Package :" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="4" Name="PackageSizeText" Margin="0,8,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Detection Rules Review -->
                <Border Background="#F8F9FA" CornerRadius="6" Padding="20" Margin="0,0,0,15"
                        BorderBrush="#E1E8ED" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="🔍 Detection Rules" FontWeight="SemiBold" 
                                   FontSize="16" Margin="0,0,0,15"/>
                        <ItemsControl Name="DetectionRulesList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Margin="0,4" Foreground="#555"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Groups Preview -->
                <Border Background="#E8F4FD" BorderBrush="#B3D9F2" BorderThickness="1"
                        CornerRadius="6" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="👥 Assignment Groups (Will be created automatically)" 
                                   FontWeight="SemiBold" FontSize="16" 
                                   Foreground="#1976D2" Margin="0,0,0,15"/>

                        <TextBlock Text="Install Groups:" FontWeight="Medium" 
                                   Foreground="#27AE60" Margin="0,0,0,5"/>
                        <TextBlock Name="SysInstallGroupText" FontFamily="Consolas" 
                                   FontSize="12" Margin="10,2"/>
                        <TextBlock Name="UsrInstallGroupText" FontFamily="Consolas" 
                                   FontSize="12" Margin="10,2"/>

                        <TextBlock Text="Uninstall Groups:" FontWeight="Medium" 
                                   Foreground="#E74C3C" Margin="0,10,0,5"/>
                        <TextBlock Name="SysUninstallGroupText" FontFamily="Consolas" 
                                   FontSize="12" Margin="10,2"/>
                        <TextBlock Name="UsrUninstallGroupText" FontFamily="Consolas" 
                                   FontSize="12" Margin="10,2"/>
                    </StackPanel>
                </Border>

                <!-- Upload Progress -->
                <!-- Upload Progress -->
                <Border Name="ProgressPanel" Visibility="Collapsed" 
                Background="White" BorderBrush="#E1E8ED" BorderThickness="1"
                CornerRadius="6" Padding="20">
                                <StackPanel>
                                    <TextBlock Text="Upload Progress" FontWeight="SemiBold" 
                           FontSize="14" Margin="0,0,0,15"/>

                                    <!-- Progress bar with percentage overlay -->
                                    <Grid Height="20" Margin="0,0,0,10">
                                        <ProgressBar Name="UploadProgressBar" Height="20"
                                 Minimum="0" Maximum="100" Value="0"
                                 Foreground="#27AE60" Background="#E0E0E0"/>
                                        <TextBlock Name="ProgressPercentageText" 
                               Text="0%" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="White"/>
                                    </Grid>

                                    <!-- Chunk status text -->
                        <TextBlock Name="ProgressStatusText" 
                           Text="Preparing..." 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,10"
                           FontSize="12"
                           Foreground="#666666"/>

                        <!-- Progress Steps (if you still want them) -->
                        <ItemsControl Name="ProgressSteps">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="24"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Icon}" 
                           FontSize="16"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Text}" 
                           Margin="10,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>